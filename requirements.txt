# Seedance 视频流水线依赖

# ─── Seedance API ───
volcengine-python-sdk[ark]        # 火山引擎 Ark SDK (pip install 'volcengine-python-sdk[ark]')
fal-client>=0.4.0                 # fal.ai (备选路径, 可选)

# ─── LLM (分镜生成) ───
openai>=1.0.0                     # LLM 调用

# ─── 视频/音频处理 ───
# ffmpeg 需系统安装: brew install ffmpeg
librosa>=0.10.0                   # 音乐 BPM + 节拍检测
soundfile>=0.12.0                 # librosa 依赖
numpy>=1.24.0

# ─── 字幕 ───
pysrt>=1.1.2

# ─── 图像 ───
opencv-python>=4.8.0              # 帧提取 + 质量检测
Pillow>=10.0.0

# ─── 通用 ───
requests>=2.31.0
python-dotenv>=1.0.0
pyyaml>=6.0
aiohttp>=3.9.0
rich>=13.0.0                      # 终端美化输出
